all: ../../example/tracing/sslsniff/sslsniff index.html

index.html:
	echo "Hello, World!" > index.html

../../example/tracing/sslsniff/sslsniff:
	make -C ../../example/tracing/sslsniff

clean:
	rm -f index.html
	
