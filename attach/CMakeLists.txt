add_subdirectory(base_attach_impl)
if(BPFTIME_ENABLE_FRIDA)
  add_subdirectory(frida_uprobe_attach_impl)
endif()
if(NOT DEFINED BPFTIME_TRAP_ATTACH_BUILD_TESTS)
  set(BPFTIME_TRAP_ATTACH_BUILD_TESTS ON)
endif()
if(UNIX AND NOT APPLE AND BPFTIME_ENABLE_TRAP_UPROBE)
  # Breakpoint based uprobe backend used where frida has no devkit (riscv64)
  add_subdirectory(trap_uprobe_attach_impl)
endif()
add_subdirectory(simple_attach_impl)
if(UNIX AND NOT APPLE)
  add_subdirectory(syscall_trace_attach_impl)
  if(BPFTIME_ENABLE_FRIDA)
    add_subdirectory(text_segment_transformer)
  endif()

  if(${BPFTIME_ENABLE_CUDA_ATTACH})
    add_subdirectory(nv_attach_impl)
  endif()
  
endif()
